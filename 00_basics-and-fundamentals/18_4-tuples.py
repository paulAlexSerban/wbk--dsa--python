# tuples - lists that you can't change after initializing them
# you can't call pop, append or remove on a tuple

chars = ("a", "b", "c", "d", "e", "f")
even_numbers = (2, 4,6, 8)
void = ()
truth = (True)
# truth.insert(0) - this ill throw an error
print(even_numbers[2])